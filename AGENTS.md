# 開発プロセス
- mainブランチに直接コミットはせず、必ずチケットごとにPull Requestを作成する運用とする。

## ドキュメントの運用
### `.steering` ディレクトリ
- 各チケットの作業をする際は、実装に入る前に、`.steering` ディレクトリに `yyyymmdd-<チケット番号>-<作業内容>.md` という名前のファイルを作成し、そこに作業を進めるステップをチェックリスト形式で記録すること。
  - ひとつの作業が完了する度にチェックをつけること。もし作業を中断する場合も、どこまで作業が進んでいるかを明確にするために、チェックをつけてから終了すること。

### `docs` ディレクトリ
- `docs` 配下のドキュメントは、仕様や設計に関する確定情報である。Pull Requestを作成する際は、必要に応じてドキュメントの更新も行うこと。逆に、作業中のメモや検討中の情報は、`.steering` 配下のファイルに記録すること。
